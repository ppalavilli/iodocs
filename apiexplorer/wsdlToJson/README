

Prerequisites
-------------

   * PHP 5.2+
   * openssl, xsl extensions must be enabled


Running
-------

To generate JSON spec files for API Explorer


1. Configure wsdl/wadl file location in config/generator.ini using the wsdlLocation/wadlLocation config directive
     Files can either be copied to local filesystem or fetched remotely via http(s)

   NOTE: You can generate JSON spec for only one descriptor file at a time. 	 

2. Run "php GenerateStubs.php"	 
